import { MmcTooltipTrigger } from './tooltip-trigger.directive';

describe('MmcTooltipTrigger', () => {
	it('should create an instance', () => {
		const directive = new MmcTooltipTrigger();
		expect(directive).toBeTruthy();
	});
});
