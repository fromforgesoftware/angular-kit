import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';
import { MmcTableComponent } from './table.component';
import * as TableStories from './table.stories';

<Meta title="Organisms/Table" />

# Table

#### Complex data tables with sorting, filtering, and pagination.

<Canvas sourceState="hidden" of={TableStories.Default} />

## Features

- ✅ Sortable columns
- ✅ Filterable data
- ✅ Pagination
- ✅ Row selection
- ✅ Custom cell templates

## Import

```typescript
import { MmcTable, MmcTableHeader, MmcTableBody, MmcTableRow, MmcTableCell } from '@mmc/ui';
```

## Examples

### Basic Usage

```html
<mmc-table [data]="tableData" [columns]="columns"> </mmc-table>
```

### With Sorting

```html
<mmc-table [data]="data" [columns]="columns" [sortable]="true" (sortChange)="onSort($event)">
</mmc-table>
```

## API Reference

<ArgTypes of={MmcTableComponent} />

## Accessibility

- Proper table semantics
- Screen reader support
- Keyboard navigation for interactive elements
