# @fromforgesoftware/angular-kit — Angular library tier

Angular utilities (jsonapi/log/ui/storage/resource-state-manager) built on ts-kit.
NOT used by the forge console (which is Vue); published for Angular consumers.

## Status
Standalone build config is PENDING — was Nx-driven in the monorepo; needs an
ng-packagr setup before `npm run build` works.

## Commands
- Install: `npm install --legacy-peer-deps`
- Build: TODO (add ng-packagr)

## Boundaries
- One-line conventional commits. NEVER edit `dist/`. NEVER commit secrets. No dependabot.
